# QA Area

This directory holds demo validation and load-testing artifacts.

- `load/` is intended for k6 or script-based rate-limit checks.
- Scenario 3 uses this area to add a test that assumes the backend ceiling remains `100`.

