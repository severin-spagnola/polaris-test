# NimbusOps API

FastAPI backend for the NimbusOps SaaS application.

## Start

```bash
pip install -e .
uvicorn app.main:app --reload
```

The application creates a local SQLite database and seeds it on startup.

